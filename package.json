{
  "name": "@ph.fritsche/semantic-release-config",
  "private": "true",
  "description": "Shared semantic-release-config",
  "keywords": [
    "semantic-release",
    "semantic-release-config"
  ],
  "repository": "https://github.com/ph-fritsche/semantic-release-config.git",
  "author": "Philipp Fritsche",
  "license": "MIT",
  "peerDependencies": {
    "semantic-release": ">=17"
  },
  "devDependencies": {
    "@ph.fritsche/eslint-config": "^0.1.1",
    "eslint": "^7.15.0"
  },
  "scripts": {
    "lint": "yarn eslint ."
  },
  "dependencies": {
    "conventional-changelog-conventionalcommits": "^4.5.0"
  }
}
